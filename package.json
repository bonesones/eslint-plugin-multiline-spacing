{
  "name": "eslint-plugin-multiline-spacing",
  "version": "1.1.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "release:patch": "npm version patch -m \"chore(release): %s\" && git push && git push --tags && npm publish",
    "release:minor": "npm version minor -m \"chore(release): %s\" && git push && git push --tags && npm publish",
    "release:major": "npm version major -m \"chore(release): %s\" && git push && git push --tags && npm publish"
  },
  "keywords": [
    "eslint",
    "eslintplugin",
    "multiline",
    "jsx",
    "spacing"
  ],
  "peerDependencies": {
    "eslint": ">=8.0.0"
  }
}
